version: "3.9"

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - DATABASE_URL=jdbc:postgresql://db:5432/librosdb
      - DB_USER=librosuser
      - DB_PASS=librospass
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-net

  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=librosdb
      - POSTGRES_USER=librosuser
      - POSTGRES_PASSWORD=librospass
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U librosuser -d librosdb"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - app-net

volumes:
  postgres-data:

networks:
  app-net: